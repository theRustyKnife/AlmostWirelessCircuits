local config = {}

-- how many ticks between checking no copper wires are connected
config.REFRESH_RATE = 10

-- the name of our entity
config.POLE_NAME = "almost-an-antenna"

-- the reach of the poles, reduce for less lag when placing
config.MAX_DISTANCE = 500

return config